server {
    listen 80;
    server_name 148.230.70.161;

    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # --- Ship observation CSVs ---
    # Accessible at: http://148.230.70.161/data/ships/<WMOID>.csv
    location /data/ships/ {
        alias /opt/arct-collector/data/processed/csv/ships/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- SIMBA ice buoy CSVs ---
    location /data/simba/ {
        alias /opt/arct-collector/data/processed/csv/simba/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- Thermistor chain buoy CSVs ---
    location /data/thermistor/ {
        alias /opt/arct-collector/data/processed/csv/thermistor/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- ArctSum 2025 buoy CSVs ---
    location /data/arctsum/ {
        alias /opt/arct-collector/data/processed/csv/arctsum/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- SvalMIZ 2026 buoy CSVs ---
    location /data/svalmiz/ {
        alias /opt/arct-collector/data/processed/csv/svalmiz/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- IABP buoy CSVs + index ---
    location /data/iabp/ {
        alias /opt/arct-collector/data/processed/csv/iabp/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- Svalbard & Jan Mayen land-station CSVs ---
    location /data/svalbard/ {
        alias /opt/arct-collector/data/processed/csv/svalbard/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- Northern Norway land-station CSVs ---
    location /data/north_norway/ {
        alias /opt/arct-collector/data/processed/csv/north_norway/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- Offshore platform CSVs ---
    location /data/offshore/ {
        alias /opt/arct-collector/data/processed/csv/offshore/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- DMI Greenland station CSVs ---
    location /data/greenland/ {
        alias /opt/arct-collector/data/processed/csv/greenland/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- AROME Arctic verification JSON ---
    location /data/arome/ {
        alias /opt/arct-collector/data/processed/csv/arome/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type application/json;
    }

    # --- ECCC Canadian Arctic station CSVs ---
    location /data/canada/ {
        alias /opt/arct-collector/data/processed/csv/canada/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- ECMWF IFS / AIFS verification JSON ---
    location /data/ecmwf/ {
        alias /opt/arct-collector/data/processed/csv/ecmwf/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type application/json;
    }

    # --- Alaska ASOS station CSVs ---
    location /data/alaska/ {
        alias /opt/arct-collector/data/processed/csv/alaska/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- Russian Arctic / FJL / NZ station CSVs ---
    location /data/russia/ {
        alias /opt/arct-collector/data/processed/csv/russia/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- Iceland IMO station CSVs ---
    location /data/iceland/ {
        alias /opt/arct-collector/data/processed/csv/iceland/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- Finland FMI station CSVs ---
    location /data/finland/ {
        alias /opt/arct-collector/data/processed/csv/finland/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- Sweden SMHI station CSVs ---
    location /data/sweden/ {
        alias /opt/arct-collector/data/processed/csv/sweden/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

    # --- MET Norway buoy CSVs ---
    location /data/norway_buoys/ {
        alias /opt/arct-collector/data/processed/csv/norway_buoys/;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "no-cache";
        default_type text/plain;
    }

}
